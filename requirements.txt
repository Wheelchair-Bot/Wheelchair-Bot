# DEPRECATED — use requirements/sim.in or requirements/pi.in directly.
# See requirements/README.md.
# Kept for backwards compatibility with existing `pip install -r requirements.txt` callers.
-r requirements/sim.in
